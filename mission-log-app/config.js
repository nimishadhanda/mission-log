// Fill these in once your Supabase project exists (Project Settings → API).
// The "anon" key is meant to be public in client-side code like this — it's
// only allowed to do what the RLS policies in supabase-schema.sql permit.
window.SUPABASE_URL = "YOUR_SUPABASE_PROJECT_URL";
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
